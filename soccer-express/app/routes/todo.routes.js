module.exports = function(app) {
    app.use((req, res, next) => {
        res.header(

        )
    });
    app.get("", () => {})
    app.post("", () => {})
    app.update("", () => {})
    app.delete("", () => {})
}